BASMAT ICT — QUOTATION PRINT LAYOUT BALANCE
============================================

NO SQL. NO DATABASE CHANGES.

FIX:
- DESCRIPTION gets the largest space.
- DESCRIPTION is multiline and wraps long BOQ text.
- QTY and UNIT are compact.
- Price/VAT/Total columns are balanced.
- Print uses fixed column widths to stop random spreading.
- QR Verification, IBAN, UNIT saving, Save and Email remain unchanged.

INSTALL:
cd C:\projects\Basmat-ICT
Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-HOTFIX-Quotation-Print-Layout-Balance.zip" -DestinationPath . -Force
npm run dev

TEST:
1. Open a quotation with a long DESCRIPTION.
2. Verify DESCRIPTION has visibly more width than UNIT/QTY.
3. Print/PDF preview.
4. DESCRIPTION should wrap cleanly.
5. UNIT/QTY should no longer consume excessive width.
6. Confirm QR + QVR code + IBAN remain visible.
