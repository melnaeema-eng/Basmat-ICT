BASMAT ICT — INVENTORY SCHEMA MATCH HOTFIX
===========================================

NO SQL. NO DATABASE CHANGES.

CONFIRMED ict_inventory_items SCHEMA:
item_no, sku, name, category, unit, warehouse_id, supplier_id,
quantity_on_hand, reorder_level, average_cost, serial_tracked,
status, notes, created_at, updated_at.

FIX:
- Removed invalid item_type
- Removed invalid standard_cost
- Removed invalid serial_tracking
- Uses average_cost
- Uses serial_tracked
- Uses quantity_on_hand
- Keeps reorder_level/status/notes

INSTALL:
cd C:\projects\Basmat-ICT
Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-HOTFIX-Inventory-Schema-Match.zip" -DestinationPath . -Force
npm run dev

TEST:
/admin/inventory
Create a new inventory item and verify it saves without schema-cache errors.
