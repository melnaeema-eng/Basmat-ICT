BASMAT ERP — SPRINT 30 BIG
CONTRACT LIFECYCLE + SLA + RECURRING BILLING + RENEWALS
=======================================================

BASELINE:
Sprint 29 Stable

STEP 1 — SUPABASE
Run ONE file only:
supabase\SPRINT30_ALL_IN_ONE.sql

Contains:
PRECHECK -> MIGRATION -> HEALTH -> SAFE SQL EDITOR VALIDATION -> FINAL RESULT

No auth-protected RPC is executed from SQL Editor.

REQUIRED FINAL:
SPRINT 30 ALL-IN-ONE | PASS ✅

If PRECHECK fails:
STOP. Send exact error only.

STEP 2 — FRONTEND
cd C:\projects\Basmat-ICT

Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-Sprint-30-Contract-Lifecycle-SLA-Recurring-Billing-ERP.zip" -DestinationPath . -Force

npm run dev

STEP 3 — TEST
1. /admin/contract-lifecycle opens.
2. Sidebar shows العقود وSLA والفوترة المتكررة.
3. Open module is orange.
4. Add contract line.
5. Add SLA policy.
6. Generate recurring billing schedule.
7. Run renewal scan.
8. Billing tab shows schedules.
9. Renewals tab shows renewal pipeline.
10. /admin/order-to-cash still opens.
11. /admin/financial-control still opens.
12. /admin/treasury still opens.
13. /admin/financial-planning still opens.
14. npm run build.

STEP 4 — GITHUB AFTER ALL PASS
git add .
git commit -m "Sprint 30 Contract Lifecycle SLA Recurring Billing ERP"
git push origin main
