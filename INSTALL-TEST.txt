ADMIN MOBILE LOGOUT HOTFIX
===========================

NO SQL. NO SUPABASE CHANGES.

cd C:\projects\Basmat-ICT
Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-HOTFIX-Admin-Mobile-Logout.zip" -DestinationPath . -Force
npm run dev

TEST ON MOBILE:
1. Login to Admin.
2. Open hamburger/sidebar.
3. Scroll to bottom.
4. "تسجيل الخروج" must appear above "العودة إلى الموقع".
5. Tap it -> session signs out and protected admin routing returns to login.
