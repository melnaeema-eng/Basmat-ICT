CUSTOMER RESET PASSWORD HOTFIX

Changes ONLY:
src/contexts/CustomerAuthContext.jsx

Production reset redirect:
https://ict.basmat-alnawabig.com.sa/portal/reset-password

PowerShell:

cd C:\projects\Basmat-ICT

Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-Customer-Reset-HOTFIX.zip" -DestinationPath . -Force

npm run dev

NO SQL.
NO Edge Function.
DO NOT push GitHub until a NEW customer reset email is tested.

IMPORTANT:
After deploying to production, request a completely NEW reset email.
Old Supabase recovery links must not be reused.
