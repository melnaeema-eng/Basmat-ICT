RESTORE CUSTOMER RESET TO SPRINT 16 STABLE

This restores ONLY:
src/contexts/CustomerAuthContext.jsx

PowerShell:
cd C:\projects\Basmat-ICT

Expand-Archive -Path "$env:USERPROFILE\Downloads\Basmat-ICT-RESET-RESTORE-S16.zip" -DestinationPath . -Force

npm run dev

Then deploy to production:
git add .
git commit -m "Restore customer password reset flow from Sprint 16 stable"
git push origin main

After Cloudflare deploy finishes:
Request a completely NEW customer password reset email from the production website.

No SQL.
No Edge Function.
No changes to Sprint 17 pages.
